<?xml version="1.0" encoding="utf-8"?>
<!--
  Plain white behind the illustration.

  The source icon is black line art on a white badge, so a white ground lets the
  launcher's mask cut the badge shape instead of stacking a rounded square
  inside a rounded mask.
-->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">
    <path
        android:fillColor="#FFFFFF"
        android:pathData="M0,0h108v108h-108z" />
</vector>
